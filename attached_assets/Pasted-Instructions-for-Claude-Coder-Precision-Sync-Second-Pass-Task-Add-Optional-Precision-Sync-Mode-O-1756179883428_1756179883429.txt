Instructions for Claude Coder: Precision Sync Second Pass
Task: Add Optional "Precision Sync" Mode
Objective: Enhance the existing stretch method with segment-based refinement for better lip sync while keeping current stretch as default.
Implementation Plan
1. Add User Interface Option

In simple_app.py sidebar: Add checkbox "Enable Precision Sync" (default: unchecked)
Show additional info: "Analyzes speech patterns for better lip sync (slower processing)"
Pass precision_sync boolean to video processor

2. Create New Method in video_processor.py
pythondef precision_sync_audio(self, video_path, new_audio_path, output_dir):
    # This will be the new precision method
3. Core Algorithm Logic:
Step A: Extract and Analyze Original Audio

Extract original audio using existing FFmpeg command
Use pydub to detect silence segments: detect_nonsilent() with threshold
Create segment map: [(start_time, end_time, 'speech'/'silence'), ...]

Step B: Analyze TTS Audio Same Way

Load TTS audio with pydub
Detect segments using same silence threshold
Create matching segment structure

Step C: Intelligent Mapping

Map TTS speech segments to original speech segments proportionally
Preserve original silence durations exactly
Calculate individual tempo ratios for each speech segment:
segment_tempo = tts_segment_duration / original_segment_duration

Step D: Reconstruction with FFmpeg

For each segment, apply individual atempo filter
Use FFmpeg's concat filter to reassemble segments
Maintain original silence timing between segments

4. Integration Points

Modify replace_audio() method: Add precision_sync=False parameter
If precision_sync is True, call new method instead of basic stretch
Keep existing stretch as fallback if precision sync fails

5. Progress Tracking Enhancement

Basic stretch: 25% → 100% (current)
Precision sync: 25% (extract) → 50% (analyze) → 75% (segment processing) → 100% (assembly)

6. Error Handling

If segment detection fails, automatically fall back to basic stretch
Handle edge cases: very short segments, no clear speech patterns
Add user feedback: "Using precision sync" vs "Falling back to basic stretch"

Expected Workflow

User enables precision sync checkbox
Processing uses segment-based stretching instead of uniform stretching
Each speech segment gets individually time-adjusted
Original pause timing is preserved exactly
Result: Much closer lip sync while maintaining audio quality

Files to Modify

simple_app.py - Add precision sync UI option
video_processor.py - Add precision sync method and integration
Add dependency: pydub (already in project)

Key Technical Requirements

Use pydub.silence.detect_nonsilent() for segment detection
Apply FFmpeg atempo filters per segment (chain multiple if needed)
Preserve exact silence timing from original audio
Fallback gracefully to basic stretch method